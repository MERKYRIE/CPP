#pragma once

#include <iostream>
#include <vector>

namespace CPP
{
    template<typename Type> std::string Spacing(const std::vector<Type>& Vector , bool Additive , unsigned long long int Spacing);
}

#include "Manipulator.inl"